version: "2.1"

extends: ./project-dependencies-rhbop.yaml

dependencies:
  - project: kiegroup/drools
    mapping:
      dependencies:
        default:
          - source: 8.37.x
            target: main
      dependant:
        default:
          - source: main
            target: 8.37.x
