version: "2.1"

extends: ./project-dependencies-rhbop.yaml

dependencies:
  - project: kiegroup/drools
    mapping:
      dependencies:
        default:
          - source: 8.39.x
            target: main
      dependant:
        default:
          - source: main
            target: 8.39.x
