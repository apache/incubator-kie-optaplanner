version: "2.1"

extends: ./project-dependencies-rhbop.yaml

dependencies:
  - project: kiegroup/drools
    mapping:
      dependencies:
        default:
          - source: 8.29.x
            target: 8.38.x-prod
      dependant:
        default:
          - source: 8.38.x-prod
            target: 8.29.x
